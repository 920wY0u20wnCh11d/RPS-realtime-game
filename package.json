{
  "name": "rps-realtime-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:vercel-config": "node scripts/validate-vercel-config.mjs",
    "dev": "npm run check:vercel-config && vercel dev",
    "start": "npm run check:vercel-config && vercel dev"
  },
  "dependencies": {
    "pusher": "^5.2.0"
  }
}
